<!-- Main Navigation -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/home"><img src="/Assets/images/arena-darkblue-updated.png" style="width:300px"></a>
    <link rel="shortcut icon" type="image/png" href="/Assets/images/favicon.png"/>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

        <% if(title == "Home") { %>
        <li class="nav-item active">
        <% } else { %>
        <li class="nav-item">
        <% } %>
          <a class="nav-link"  href="/home"><i class="fas fa-home"></i>Home</a>
          <!-- <i class="fas fa-3x fa-laptop-house"></i> -->
        </li>

         <% if(title == "Tournament List") { %>
        <li class="nav-item active">
        <% } else { %>
        <li class="nav-item">
        <% } %>
          <a class="nav-link" aria-current="page" href="/tournaments"><i class="fas fa-trophy"></i>Tournaments</a>
          <!-- <i class="fas fa-3x fa-info-circle"></i> -->
        </li>

        <!--Space reserved for additional Nav items-->

        <% if(typeof displayName == 'string' && displayName) { %>
          <li class="nav-text"><i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %> </li>
          <li class="nav-item"><a href="/logout" class="nav-link">
            <i class="fas fa-lg fa-sign-out-alt"></i> Logout </a></li>
          <% } else { %>
            <li class="nav-item"><a href="/login" class="nav-link">
              <i class="fas fa-lg fa-sign-in-alt"></i> Login </a></li>
          <% } %>

    </div>
  </div>
</nav>